/*
** EPITECH PROJECT, 2021
** include [WSL: Ubuntu]
** File description:
** scene.h
*/

#ifndef ADDON_H
    #define ADDON_H

////////////////////////////////////////////////////////////
// Headers
////////////////////////////////////////////////////////////

    #include "../game_engine.h"

////////////////////////////////////////////////////////////
/// \brief Renvoie la donnée de l'addon stocker dans l'object
///
/// \param name Le nom de l'addon
///
/// \param name L'object qui contient l'addon
///
/// \return la donnée de l'addon, si elle existe pas elle renvoie NULL
///
////////////////////////////////////////////////////////////
void *get_addon_data(char const *name, object_t *object);

////////////////////////////////////////////////////////////
/// Function système
////////////////////////////////////////////////////////////
sfBool create_addon(char const *name, addon_t *addon, engine_t *engine);
////////////////////////////////////////////////////////////
/// Function système
////////////////////////////////////////////////////////////
sfBool add_addon(char const *name, object_t *object, engine_t *engine,
    list_t *data);
////////////////////////////////////////////////////////////
/// Function système
////////////////////////////////////////////////////////////
sfBool destroy_addons(list_t *addon, sfBool boolean);

////////////////////////////////////////////////////////////
/// Function système
////////////////////////////////////////////////////////////
int load_addons(engine_t *engine, int (*const init_addon[])(engine_t *));

////////////////////////////////////////////////////////////
/// Function système
////////////////////////////////////////////////////////////
int on_end(list_t *scene, engine_t *engine);
////////////////////////////////////////////////////////////
/// Function système
////////////////////////////////////////////////////////////
int on_start(list_t *scene, engine_t *engine);

#endif
